	<% if user_signed_in? %>
	<h1>Hi <%= current_user.name %> from <%= current_user.city %></h1>

		<% else %>
	<h1>Anywhere.travel</h1>
		<p>
			Local, help travelers. Travelers, get the best advice from locals. From <%= link_to "The Anywhere Company", "http://www.anywherecompany.com"%>
		</p>
		<p>
			<%= link_to "Sign Up Now", new_user_registration_path %>
		</p>
<% end %>
